# Issue tracker: GitHub

Issues and PRDs live as GitHub Issues. Use `gh` for issue operations and infer the repository from the current Git remote.

Apply `ready-for-agent` to approved, agent-ready tickets. Use GitHub's native issue dependencies for blocking relationships when available; otherwise include `Blocked by: #<issue>` in the issue body.

Pull requests are not treated as a triage request surface.
