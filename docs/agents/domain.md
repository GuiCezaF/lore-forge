# Domain Docs

Before working, read the root `CONTEXT.md` and relevant ADRs under `docs/adr/`.

This is a single-context repository. Use the glossary's canonical vocabulary in issues, implementation, tests, and documentation. Surface conflicts with existing ADRs explicitly rather than silently overriding them.
