# Drizzle

Preparação inicial para persistência via Drizzle.

Arquivos:

- `0001_initial.sql` cria as tabelas base de `users` e `auth_sessions`

As migrations ainda não foram executadas neste ambiente.

