# URL base da API (server e client components)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Início do OAuth Google (link "Entrar com Google" na landing)
NEXT_PUBLIC_GOOGLE_AUTH_URL=http://localhost:3000/auth/google

# WebSocket da API (futuro — tempo real)
# NEXT_PUBLIC_WS_URL=ws://localhost:3000

# Anúncios — tier Free (futuro)
# NEXT_PUBLIC_GOOGLE_ADS_CLIENT=
# NEXT_PUBLIC_ADS_ENABLED=false

# Analytics (futuro)
# NEXT_PUBLIC_ANALYTICS_ENABLED=false
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
