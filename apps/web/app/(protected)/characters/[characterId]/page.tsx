"use client";

import { FormEvent, useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { Link, useRouter } from "@/i18n/navigation";
import { apiFetch } from "@/lib/api-client";
import { getBrowserApiUrl } from "@/lib/api-url";

type Character = { id: string; name: string; kind: "pc" | "npc"; status: string; campaignId: string | null; ownerUserId: string | null; sheetLabel: string | null; nex: number; origin: string | null; characterClass: string | null; path: string | null; concept: string | null; derived: Record<string, number | null> };
type Inventory = { id: string; name: string; quantity: number; isEquipped: boolean; notes: string | null };
type PlayState = { currentHp: number; currentSan: number; currentEp: number; conditions: string; temporaryEffects: string; gmNotes: string | null; inventory: Inventory[] };
type AuthUser = { id: string };
type CampaignDetail = { ownerUserId: string | null; members: Array<{ userId: string; role: "gm" | "player" | "spectator" }> };

export default function CharacterDetailPage() {
  const params = useParams(); const router = useRouter(); const id = String(params.characterId); const apiUrl = getBrowserApiUrl();
  const [character, setCharacter] = useState<Character | null>(null); const [state, setState] = useState<PlayState | null>(null); const [error, setError] = useState("");
  const [currentUser, setCurrentUser] = useState<AuthUser | null>(null); const [canManageCampaign, setCanManageCampaign] = useState(false);
  const [item, setItem] = useState({ name: "", quantity: 1, notes: "" });
  async function load() { const response = await apiFetch(`${apiUrl}/characters/${id}`); if (!response.ok) { setError("Sheet unavailable."); return; } const sheet = await response.json() as Character; setCharacter(sheet); const meResponse = await apiFetch(`${apiUrl}/auth/me`); const me = meResponse.ok ? await meResponse.json() as AuthUser : null; setCurrentUser(me); if (sheet.campaignId) { const [play, campaign] = await Promise.all([apiFetch(`${apiUrl}/characters/${id}/play-state`), apiFetch(`${apiUrl}/campaigns/${sheet.campaignId}`)]); if (play.ok) setState(await play.json() as PlayState); if (campaign.ok && me) { const detail = await campaign.json() as CampaignDetail; setCanManageCampaign(detail.ownerUserId === me.id || detail.members.some(member => member.userId === me.id && member.role === "gm")); } } }
  useEffect(() => { void load(); }, [id]); // eslint-disable-line react-hooks/exhaustive-deps
  async function saveState(event: FormEvent) { event.preventDefault(); if (!state) return; const response = await apiFetch(`${apiUrl}/characters/${id}/play-state`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ currentHp: Number(state.currentHp), currentSan: Number(state.currentSan), currentEp: Number(state.currentEp), conditions: state.conditions, temporaryEffects: state.temporaryEffects }) }); if (!response.ok) setError("Only the sheet owner can update campaign state."); else void load(); }
  async function addItem(event: FormEvent) { event.preventDefault(); const response = await apiFetch(`${apiUrl}/characters/${id}/inventory`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: item.name, quantity: Number(item.quantity), notes: item.notes }) }); if (!response.ok) { setError("Only the GM can add campaign inventory."); return; } setItem({ name: "", quantity: 1, notes: "" }); void load(); }
  async function removeItem(inventoryId: string) { const response = await apiFetch(`${apiUrl}/characters/${id}/inventory/${inventoryId}`, { method: "DELETE" }); if (!response.ok) setError("Only the GM can remove campaign inventory."); else void load(); }
  async function copy() { const label = window.prompt("Private label for the copied sheet:", character?.sheetLabel ?? ""); if (label === null) return; const response = await apiFetch(`${apiUrl}/characters/${id}/copy`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ sheetLabel: label }) }); if (response.ok) router.push("/characters"); else setError("Only the sheet owner can copy this sheet."); }
  async function archive() { if (!window.confirm("Archive this campaign sheet? It can only be copied afterwards.")) return; const response = await apiFetch(`${apiUrl}/characters/${id}/archive`, { method: "POST" }); if (response.ok) void load(); else setError("Only the sheet owner can archive this sheet."); }
  if (!character) return <p className="py-8 text-sm text-zinc-500">Loading sheet… {error}</p>;
  const isOwner = character.ownerUserId === currentUser?.id;
  return <main className="mx-auto flex w-full max-w-5xl flex-col gap-6 py-4"><Link href="/characters" className="text-xs uppercase tracking-widest text-zinc-500">← Sheets</Link><header className="flex flex-wrap justify-between gap-3"><div><p className="text-xs uppercase tracking-widest text-red-400">{character.kind === "pc" ? "Player character" : "NPC"}</p><h1 className="font-serif text-3xl text-zinc-100">{character.name}</h1><p className="mt-1 text-sm text-zinc-500">{character.characterClass ?? "Narrative NPC"} · {character.path ?? "No path"} · NEX {character.nex}%</p></div>{character.kind === "pc" && isOwner && <div className="flex gap-2"><button onClick={() => void copy()} className="rounded border border-zinc-700 px-3 py-2 text-sm text-zinc-200">Copy sheet</button>{character.status !== "archived" && <button onClick={() => void archive()} className="rounded border border-red-900 px-3 py-2 text-sm text-red-300">Archive</button>}</div>}</header>{error && <p className="rounded border border-red-900 bg-red-950/20 p-3 text-sm text-red-200">{error}</p>}<section className="grid gap-3 rounded-xl border border-zinc-800 bg-zinc-950 p-5 sm:grid-cols-4">{Object.entries(character.derived ?? {}).map(([label, value]) => <div key={label}><p className="text-[10px] uppercase tracking-widest text-zinc-600">{label.replace(/[A-Z]/g, m => ` ${m}`)}</p><p className="text-lg text-zinc-100">{value ?? "—"}</p></div>)}</section>{state && <div className="grid gap-6 lg:grid-cols-2">{isOwner ? <form onSubmit={saveState} className="space-y-4 rounded-xl border border-zinc-800 bg-zinc-950 p-5"><h2 className="font-serif text-xl text-zinc-100">Campaign state</h2><div className="grid grid-cols-3 gap-3">{(["currentHp", "currentSan", "currentEp"] as const).map(key => <label key={key} className="grid gap-1 text-xs uppercase text-zinc-500">{key}<input type="number" value={state[key]} onChange={e => setState({ ...state, [key]: Number(e.target.value) })} className="rounded border border-zinc-700 bg-zinc-900 p-2 text-sm text-white" /></label>)}</div><label className="grid gap-1 text-xs uppercase text-zinc-500">Conditions<textarea value={state.conditions} onChange={e => setState({ ...state, conditions: e.target.value })} className="rounded border border-zinc-700 bg-zinc-900 p-2 text-sm text-white" /></label><label className="grid gap-1 text-xs uppercase text-zinc-500">Temporary effects<textarea value={state.temporaryEffects} onChange={e => setState({ ...state, temporaryEffects: e.target.value })} className="rounded border border-zinc-700 bg-zinc-900 p-2 text-sm text-white" /></label><button className="rounded bg-zinc-700 px-3 py-2 text-sm text-white">Save state</button></form> : <section className="rounded-xl border border-zinc-800 bg-zinc-950 p-5 text-sm text-zinc-500">Only the sheet owner can change current resources and conditions.</section>}<section className="space-y-4 rounded-xl border border-zinc-800 bg-zinc-950 p-5"><h2 className="font-serif text-xl text-zinc-100">Campaign inventory</h2><ul className="space-y-2">{state.inventory.map(entry => <li key={entry.id} className="flex items-center justify-between rounded border border-zinc-800 p-2 text-sm text-zinc-300"><span>{entry.name} × {entry.quantity}{entry.notes ? ` — ${entry.notes}` : ""}</span>{canManageCampaign ? <button onClick={() => void removeItem(entry.id)} className="text-xs text-red-300">Remove</button> : null}</li>)}</ul>{canManageCampaign ? <form onSubmit={addItem} className="grid gap-2"><input required placeholder="Item name" value={item.name} onChange={e => setItem({ ...item, name: e.target.value })} className="rounded border border-zinc-700 bg-zinc-900 p-2 text-sm text-white" /><input type="number" min="1" value={item.quantity} onChange={e => setItem({ ...item, quantity: Number(e.target.value) })} className="rounded border border-zinc-700 bg-zinc-900 p-2 text-sm text-white" /><input placeholder="GM note" value={item.notes} onChange={e => setItem({ ...item, notes: e.target.value })} className="rounded border border-zinc-700 bg-zinc-900 p-2 text-sm text-white" /><button className="rounded bg-red-700 px-3 py-2 text-sm text-white">Add item (GM)</button></form> : <p className="text-xs text-zinc-500">Only the GM can manage campaign inventory.</p>}</section></div>}{!character.campaignId && <p className="rounded border border-zinc-800 p-4 text-sm text-zinc-500">This unassigned sheet has no campaign-specific inventory or current state yet.</p>}</main>;
}
